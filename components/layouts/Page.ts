import styled from 'styled-components';
import tw from 'twin.macro';

const Page = styled.div`
  ${tw`
    font-sans
    w-full
    max-w-[900px]
    p-[16px]
    m-auto
  `}
`;

export default Page;
